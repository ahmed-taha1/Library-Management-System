# Server
PORT=3000

# Database (PostgreSQL)
CONNECTION_STRING=postgresql://user:password@localhost:5432/library_db?schema=public

# JWT Authentication
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRATION=1h

# Refresh Token (in milliseconds, default: 7 days)
REFRESH_TOKEN_EXPIRATION=604800000
